server:
  port: 8081

# Configurar las rutas donde se encuentran -
# - los archivos de configuracion de los microservicios

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Mauricio0529/microservicios.git
          searchPath: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t
